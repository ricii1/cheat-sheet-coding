[Unit]
Description=Laravel Artisan Serve
After=network.target

[Service]
User=www-data
Group=www-data
Restart=always
WorkingDirectory=/path/to/laravel
ExecStart=/usr/bin/php /path/to/laravel/artisan serve --host=0.0.0.0 --port=8000

[Install]
WantedBy=multi-user.target
